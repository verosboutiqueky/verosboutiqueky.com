<!-- TEMP: REMOVE AFTER OPENING -->
<div class="opening-banner">
  <div class="opening-banner__content">
    <span class="opening-banner__text">Opening February 2026</span>
  </div>
</div>

<style>
  .opening-banner {
    background: linear-gradient(180deg, rgba(245, 241, 233, 0.55) 0%, rgba(245, 241, 233, 0.35) 100%);
    border-top: 1px solid var(--color-gold);
    border-bottom: 1px solid var(--color-gold);
    padding: 1rem 1.5rem;
    text-align: center;
  }

  .opening-banner__content {
    display: flex;
    flex-direction: row;
    gap: 0;
    font-family: var(--font-serif);
    font-size: clamp(0.875rem, 2.5vw, 1.125rem);
    font-weight: 400;
    letter-spacing: 0.15em;
    text-transform: uppercase;
    color: var(--color-black);
    justify-content: center;
    flex-wrap: wrap;
  }

  .opening-banner__text {
    display: block;
    line-height: 1.2;
  }

  @media (min-width: 640px) {
    .opening-banner {
      padding: 1.125rem 2rem;
    }
  }

  @media (min-width: 1024px) {
    .opening-banner {
      padding: 2.25rem 3rem;
    }
  }
</style>
